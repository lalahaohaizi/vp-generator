# �������������ڵ��� DeepSeek / OpenAI ���ݽӿ�
openai>=1.0.0
pydantic>=2.0.0

# Agentic layer (optional — pmc_patients falls back to substring ranking without them)
# datasets  # for HF download helper (huggingface-cli)
# rank-bm25 # for BM25 when FTS5 is unavailable